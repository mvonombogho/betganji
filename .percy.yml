version: 2
snapshot:
  widths:
    - 375 # Mobile
    - 768 # Tablet
    - 1280 # Desktop
  minHeight: 1024
  percyCSS: |
    // Hide dynamic content that changes between snapshots
    [data-cy=current-time] { visibility: hidden; }
    [data-cy=odds-value] { visibility: hidden; }