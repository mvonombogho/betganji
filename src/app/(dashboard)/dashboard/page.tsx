export function DashboardPageDisabled() {
  // This component is currently disabled to avoid route conflicts 
  // with src/app/dashboard/page.tsx
  return null;
}

// We're not exporting as default to prevent Next.js from using it as a page
// export default DashboardPageDisabled;
